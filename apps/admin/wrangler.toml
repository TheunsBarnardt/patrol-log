# Cloudflare Pages — deploy with:
#   pnpm run build
#   wrangler pages deploy dist --project-name patrol-log-admin --commit-dirty=true
# Production API calls go through functions/api (same-origin /api proxy).
name = "patrol-log-admin"
pages_build_output_dir = "dist"
compatibility_date = "2024-11-06"
